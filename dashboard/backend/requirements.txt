fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1
httpx==0.27.2
websockets==13.1

# Optional / production integrations.
# Pipecat and vendor SDKs are intentionally NOT pinned here so the project
# installs and runs cleanly in MOCK_MODE without heavy native dependencies.
# Uncomment for production:
# pipecat-ai==0.0.47
# twilio==9.3.0
# boto3==1.35.0          # AWS Bedrock Nova Sonic
